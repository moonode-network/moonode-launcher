<!--
  ~ Moonode Launcher
  ~ Copyright (C) 2025 Moonode
  ~
  ~ Based on FLauncher by Étienne Fesser (GPL-3.0)
  ~ Modified for Moonode TV integration
  -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.moonode.launcher">

    <!-- TV Features -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <!--
      android.software.leanback is marked required="false" so the APK can also
      install on Amazon Fire TV (Fire OS does not declare the leanback feature).
      Android TV devices still expose leanback, so the LEANBACK_LAUNCHER intent
      filter below keeps surfacing us in the Android TV home row exactly as before.
    -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature 
        android:name="android.hardware.faketouch"
        android:required="false"/>
    <uses-feature 
        android:name="android.hardware.telephony"
        android:required="false"/>
    <uses-feature 
        android:name="android.hardware.camera"
        android:required="false"/>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
      FOREGROUND_SERVICE keeps the launcher process alive on aggressive OEM
      killers (Xiaomi MIUI in particular). Without it, MIUI wipes the
      Moonode process the moment the operator opens Settings, and the WebView
      reloads from scratch on return. POST_NOTIFICATIONS is required on
      Android 13+ to surface the silent keep-alive notification; we don't
      block on it (the launcher works fine if denied), it just falls back
      to non-foreground service priority.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Required on Android 14+ (targetSdk 34) when foregroundServiceType=specialUse. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Media Permissions - Audio/Video autoplay -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!--
      WRITE_SECURE_SETTINGS lets the launcher silently re-enable the HOME
      Guardian accessibility service whenever Fire OS disables it (which it
      does on every APK reinstall and after some OS updates). This is a
      privileged permission that ONLY takes effect once it is granted via
      ADB at deployment time:

        adb shell pm grant com.moonode.launcher android.permission.WRITE_SECURE_SETTINGS

      The deployment script (scripts/setup-moonode-launcher.{sh,ps1,bat})
      runs this command automatically. If the permission is not granted,
      the auto-recovery silently no-ops; the launcher still works fine,
      it just can't self-heal the HOME Guardian without manual ADB.
    -->
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
        <intent>
            <action android:name="android.intent.action.GET_CONTENT" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:banner="@drawable/banner"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Moonode Launcher"
        android:usesCleartextTraffic="true"
        android:hardwareAccelerated="true"
        android:largeHeap="true">
        
        <!-- Main Activity - HOME Launcher -->
        <!--
          NOTE on launcher activity flags:
          - launchMode="singleTask": one MainActivity per task, HOME intent
            routes to onNewIntent on the existing instance (no recreation).
          - clearTaskOnLaunch was REMOVED: with singleTask there is only one
            activity in the task, so it was a no-op for the back stack but
            on some OEMs (Xiaomi MiTV) it caused the system to drop the
            WebView's in-memory state on every HOME press, making the
            launcher visibly "reload" even though the SW cache on disk
            was intact. Removing it lets the WebView keep its render state
            so cached content shows instantly.
          - stateNotNeeded was REMOVED for the same reason: when the OS
            briefly recreates the activity for config changes (most are
            already in configChanges, but device-level events still slip
            through), we want the WebView to be re-attached, not reset.
        -->
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <meta-data
                android:name="io.flutter.embedding.android.SplashScreenDrawable"
                android:resource="@drawable/launch_background" />

            <!--
              HOME Launcher Intent Filters.
              priority=999 is the maximum third-party value Android honours and
              is needed to outrank Amazon Fire TV's stock HOME activities
              (com.amazon.tv.launcher = priority 950, com.amazon.firehomestarter
              = priority 1). On Android TV / AOSP this is a no-op because the
              system launcher picker is the source of truth there.
            -->
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
                <category android:name="android.intent.category.LAUNCHER_APP" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Boot Receiver - Auto-start on device boot -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!--
          LauncherKeepAliveService: foreground service that keeps the
          launcher process from being killed by aggressive OEM task
          managers (notably Xiaomi MIUI). See class comment for details.
        -->
        <service
            android:name=".LauncherKeepAliveService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="kiosk-display-keep-alive" />
        </service>

        <!--
          HOME Hijack accessibility service (Fire TV only in practice).
          Disabled by default; user must enable it from Settings -> Accessibility.
          BIND_ACCESSIBILITY_SERVICE is mandated by Android so the system can
          guarantee only the OS binds to this service.
        -->
        <service
            android:name=".HomeHijackService"
            android:exported="true"
            android:label="Moonode HOME Guardian"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
